# VESPER model validation

Status: **NOT RUN — no real historical dataset or trained model available.**

Training, walk-forward validation, locked-test dates, sample sizes, pick returns, excess return, drawdown, calibration, and baseline comparisons are unavailable. No numbers are fabricated. The candidate-training command will generate a report only after a real dataset passes input validation.

Unit tests of model fitting and artifact integrity use explicitly synthetic fixtures; they are not investment-performance evidence.
